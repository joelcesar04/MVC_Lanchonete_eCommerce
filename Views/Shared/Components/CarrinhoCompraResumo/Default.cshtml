@model CarrinhoCompraViewModel

@if (Model.CarrinhoCompra.CarrinhoCompraItens.Count > 0)
{
    <a asp-controller="CarrinhoCompra" asp-action="Index" style="text-decoration: none">
        <span><img src="~/images/icons8-carrinho-30.png" style="width: 20px" /></span>
        <span style="color: white">
            @Model.CarrinhoCompra.CarrinhoCompraItens.Count
        </span>
    </a>
}